// TODO band 203: axum server on 127.0.0.1:9998 serving ui/ + /api/fs/*, plus xterm.js terminal
fn main() -> anyhow::Result<()> {
    println!("linfs-gui not yet implemented (band 203) — use `linfs list` CLI for now");
    Ok(())
}
